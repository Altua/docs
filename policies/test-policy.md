# Test policy

## Purpose

This file is a placeholder for standalone policy content.

## Scope

Use the `policies/` folder for Markdown files that should stay outside the Mintlify documentation site.

## Notes

- Keep policy files in standard Markdown.
- Export PDFs with the local CLI tool.
- Do not add these files to `docs.json`.
